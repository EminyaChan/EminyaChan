# Copy this file to .env and fill in your own values.

# Optional but recommended: without this, the agent uses a built-in
# template generator instead of real AI drafting. Get a key at
# https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Port for the review dashboard (default 3000)
PORT=3000
